// TODO: build the message input bar
// - A textarea (not input) so the user can write multi-line messages
// - A send button (↑) that calls useChatStore.sendMessage
// - Enter sends, Shift+Enter adds a newline
// - Disable both textarea and button while isLoading is true
// - Clear the textarea after sending

export function InputBar() {
  return <div>Input bar (TODO)</div>;
}
