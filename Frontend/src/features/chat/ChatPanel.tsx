// TODO: build the chat panel as a floating bottom sheet
// It should have three height states: collapsed (64px), half (40vh), expanded (80vh)
// The user can click/drag the handle at the top to cycle between states
// Component tree:
//   ChatPanel
//   ├── drag handle (button)
//   ├── MessageList
//   └── InputBar

export function ChatPanel() {
  return <div>Chat panel (TODO)</div>;
}
