// TODO: render the conversation history
// - Read messages and isLoading from useChatStore
// - Render each message, styling user vs assistant differently
// - Show a typing indicator (animated dots) when isLoading is true
// - Auto-scroll to the bottom whenever messages or isLoading changes

export function MessageList() {
  return <div>Message list (TODO)</div>;
}
